apiVersion: v1
kind: PersistentVolume
metadata:
  name: kgs-1-production
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/k8s/volumes/kgs/1/staging"

